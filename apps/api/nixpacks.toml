[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r apps/api/requirements.txt"]

[start]
cmd = "uvicorn apps.api.main:app --host 0.0.0.0 --port $PORT"
